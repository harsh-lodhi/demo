import { View } from "react-native";
import { Text } from "react-native-paper";

const RefillScreen = () => {
  return (
    <View>
      <Text>Refill screen</Text>
    </View>
  );
};

export default RefillScreen;
