import { View } from "react-native";
import { Text } from "react-native-paper";

const IndexScreen = () => {
  return (
    <View>
      <Text>Ops home</Text>
    </View>
  );
};

export default IndexScreen;
